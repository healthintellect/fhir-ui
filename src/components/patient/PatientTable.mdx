---
name: PatientTable
menu: Components
route: /components/patient/PatientTable
---

import { Playground, Props } from 'docz'
import { ThemeProvider } from '@material-ui/core/styles'
import { Container, Grid } from '@material-ui/core'
import PatientTable from './PatientTable'
import { patientBundle } from '../../examples/fhir-examples'

# PatientTable

PatientTable displays a material-ui table component when passed an array of FHIR Patient Resources using the patients property. Different styles of fields can be used as shown below.

## Properties

<Props of={PatientTable} />

## Basic usage

<Playground>
  <Container>
    <Grid container spacing={3} direction="row">
      <Grid item xs={12}>
        <PatientTable
          patients={patientBundle.entry.map(pt => pt.resource)}
          tableTitle="Patient List"
          tableRowSize="small"
          defaultRowsPerPage={10}
        />
      </Grid>
    </Grid>
  </Container>
</Playground>
